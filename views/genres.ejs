<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
      <% genres.map((name) => { %>
      <li><%= name.genre.charAt(0).toUpperCase() + name.genre.slice(1) %></li>
      <%}) %>
    </ul>
  </body>
</html>
