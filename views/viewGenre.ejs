<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= genre.charAt(0).toUpperCase() + genre.slice(1) %> Games</title>
  </head>
  <body>
    <h1><%= genre.charAt(0).toUpperCase() + genre.slice(1) %> Games</h1>
    <ul>
      <% games.map((game) => { %>
      <li><a href="/games/<%= game.id %>"><%= game.title %></a></li>
      <%})%>
    </ul>
  </body>
</html>
